/* eslint-disable no-unused-vars */
export enum EPages {
  SEARCH = 'search',
  REQUESTS = 'requests',
  SETTINGS = 'settings',
  STATE = 'state',
  JOURNAL = 'journal'
}

export enum ELinks {
  search = 'Поиск',
  requests = 'Запросы',
  settings = 'Настройки',
  state = 'Состояние',
  journal = 'Журнал',
}

export enum EVuexNamespaces {
  CORE = 'Core'
}
